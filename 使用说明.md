# Daily Schedule 使用说明（桌面版 + 安卓版 + 同步）

## 一、桌面版（Windows）
双击运行 `dist\Daily Schedule-win32-x64\Daily Schedule.exe` 即可。
（源码在 `日程管理.html`，打包文件在 `dist\...\resources\app.asar`）

## 二、安卓版（APK）
- APK 文件：`Daily-Schedule.apk`（构建后生成）
- 安装方法：把 APK 传到手机（数据线 / 微信文件传输 / 网盘），在手机上点击安装。
  首次安装需在系统设置里允许"安装未知来源应用"。
- 手机版与桌面版功能完全一致：紧凑视图 / 大日历 / 单日管理 / 时间选择 / 云端同步。

## 三、数据同步（手机 ↔ 电脑）

应用默认**不连接任何服务器**。在 **☁ 同步** 设置里填写你自己的服务器地址（两端填**同一个地址**）后，双端**每 5 秒自动同步**：改动即时推送、另一台自动拉取更新、失败自动重试，按天合并不丢数据。

配置方式如下（任选其一）：

### 方式 A：电脑本地运行（0 注册，同一 WiFi 可用）
1. 在电脑上打开命令行，进入本项目目录，运行：
   ```
   node sync-server.js
   ```
2. 终端会显示局域网地址，例如 `http://192.168.1.5:3000`。
3. 电脑和手机的同步设置里都填这个地址（手机需与电脑连**同一个 WiFi**）。
4. 想开机自启可以双击 `启动同步服务器.bat`（已提供）。

### 方式 B：自建免费云部署（备选，已有一个在用的官方服务器可跳过）
把同步服务器部署到免费云平台，得到一个公网 `https://` 地址，手机用流量也能同步。

**用 Render 部署（推荐，无需信用卡）：**
1. 注册 Render 账号：https://render.com（邮箱即可）。
2. 把 `cloud-deploy` 文件夹里的 `sync-server.js` + `package.json` 上传到你的 GitHub 仓库（新建一个仓库，放这两个文件即可）。
3. Render 控制台 → New → Web Service → 选择该仓库。
4. 设置：
   - Runtime: **Node**
   - Build Command: 留空
   - Start Command: `node sync-server.js`
   - Instance Type: Free
5. 点 Create。约 1 分钟后状态变 Live，页面会显示 `https://xxx.onrender.com`。
6. 电脑和手机同步设置里都填 `https://xxx.onrender.com`。

**用 Railway 部署（备选）：**
```
npm i -g @railway/cli
railway login
cd 项目目录（含 sync-server.js 的文件夹）
railway init
railway up
```
完成后 `railway domain` 获取 https 地址，填入应用。

> 注意：免费平台的实例休眠后首次访问会慢几秒，属正常现象。
> 数据同时保存在各设备本地和服务器上，任一端异常都不会丢数据。

## 四、数据安全
- 日程数据默认只存本机（localStorage），开启同步后才会上传服务器。
- 服务器上的数据以 JSON 保存在 `sync-data.json`（方式 A 在项目目录；方式 B 在云盘实例里）。
- 合并规则：同一天的数据按"最后修改时间"保留较新版本；同一时间则按条目合并，不会互相覆盖丢失。

## 五、修改界面后重新构建 APK
1. 修改 `日程管理.html`（源码）后，同步到手机工程：`copy 日程管理.html mobile\www\index.html`
2. 刷新安卓工程资源：在 `mobile` 目录运行 `npx cap sync android`
3. 在 `C:\androidbuild\mobile\android` 目录运行 `gradlew.bat assembleDebug`
4. 新的 APK 在 `C:\androidbuild\mobile\android\app\build\outputs\apk\debug\app-debug.apk`，复制为 `Daily-Schedule.apk` 即可
> 注意：安卓工程必须放在纯英文路径下构建（项目路径含中文会导致 Gradle 构建失败），这就是 `C:\androidbuild` 存在的原因。`mobile\android` 目录里的工程无法直接构建，需按上面步骤在英文路径构建。
